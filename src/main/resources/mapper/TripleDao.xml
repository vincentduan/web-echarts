<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.ddy.dao.TripleDao">
	<!-- 目的：为Dao接口方法提供sql语句配置 -->
	<select id="queryAll" resultType="Triple">
		select id, arg1, rel, arg2
		from triple
	</select>
</mapper>